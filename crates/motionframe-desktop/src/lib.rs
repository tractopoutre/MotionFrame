//! `MotionFrame` desktop shell: egui app, CLI, file dialogs.
//!
//! Engine logic lives in `motionframe-engine`. Shared UI widgets live in `motionframe-ui`.

pub mod app;
pub mod cli;
pub mod locale;
