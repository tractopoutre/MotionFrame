accepted = [
    "Apache-2.0",
    "Apache-2.0 WITH LLVM-exception",
    "MIT",
    "BSD-2-Clause",
    "BSD-3-Clause",
    "ISC",
    "Zlib",
    "MPL-2.0",
    "Unicode-DFS-2016",
    "Unicode-3.0",
    "CC0-1.0",
    "CDLA-Permissive-2.0",
    "OpenSSL",
    "BSL-1.0",
    "OFL-1.1",
    "Ubuntu-font-1.0",
]

ignore-build-dependencies = true
ignore-dev-dependencies = true

# First-party workspace crates (publish = false) are MotionFrame's own code,
# not third-party libraries, so exclude them from this report.
[private]
ignore = true

# Crates whose embedded license text is missing or non-canonical can be
# clarified with `[clarify."crate-name"]` blocks here. None needed yet.
